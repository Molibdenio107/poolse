# Decisions

Product decisions taken in conversation, one line each, newest last. They live here
because the reasoning otherwise survives only in a chat log nobody reads again — and
several of these reverse an earlier decision, which is exactly the kind of thing that gets
quietly re-reversed six months later by somebody reading the older comment.

Schema decisions live in `docs/data-model.md`; standing conventions live in `CLAUDE.md`.
This file is for the calls themselves.

Most entries are one line. A sizing or pricing call that sets numbers other decisions will
be measured against gets a dated section of its own instead, because the numbers are the
decision and a line that omits them is not usable later.

## Round 5 — September 2026

- **2026-09-05** — Round 5 branches from `refinements-round-4`, not `main`. `main` is 86 commits behind and lacks the calendar, partnerships, fees and inventory screens every round-5 ticket edits.
- **2026-09-05** — The dashboard is the home route and the first menu item. Reverses POOLSE-37 and POOLSE-38, which put Instalações first on the argument that the dashboard was about *you*; round 4 moved every account question to "O meu perfil" and put occupancy there instead, so it is now about the operation.
- **2026-09-05** — POOLSE-37's per-role landing chain is deleted rather than kept. There is nothing to resolve once every role lands on the same page, and a caller-less resolver is how the old behaviour returns by accident.
- **2026-09-05** — The periodicity discount is applied on **display only**. Prices stay stored gross, and `student_fee` goes on snapshotting the amount and the discount when a family agrees a price.
- **2026-09-05** — The three capacity rules compose rather than override: an enrolment must fit its turma, the turmas sharing a slot must fit the tank, and `lane_level_capacity` goes on meaning what it meant.
- **2026-09-05** — A tank with no `max_capacity` is unlimited and enforces nothing. Null is "not measured", never zero.
- **2026-09-05** — Partnership bookings count as zero against a tank ceiling, because they carry no headcount. Revisit if partner groups ever gain one.
- **2026-09-05** — The per-session restore endpoint is reinstated, owner/admin only, so ticket 9.6's Undo can work. This reverses backlog round 3, story 5, which removed the operator-facing restore deliberately — reversed on request, not by drift.
- **2026-09-05** — Lost and found records that a student was told with a `student_notified_at` stamp on the item, not a new `student_notification` table. The notifications subsystem is phase 3.0 and is meant to be built once; a second store would be something for it to migrate away from.
- **2026-09-05** — The water-quality importer makes no model call. The mapping step stays a `MatchSpec` like the other three, so an assisted matcher slots in at one call site if it is ever wanted.
- **2026-09-05** — The water import is per tank, from the tank's own page. A row whose tank column names a different tank is refused rather than imported into whichever tank was open.
- **2026-09-05** — An out-of-range reading imports with no warning of its own; only unreadable values refuse a row. Losing the day a pool was unsafe is the worst thing an importer of safety records could do.
- **2026-09-05** — Ticket 5's wizard is built as its own leaner file; the extraction of a shared import wizard waits until there are four examples to generalise from rather than three.
- **2026-09-05** — An inventory item's location is free text with suggestions, not a rooms entity. The words the club types are the data that would tell us what rooms to model, if rooms ever earn a table.
- **2026-09-05** — The place words (local, localização, onde, location, where) moved from the inventory importer's `pools` field to the new `location` field. They were always place words; matching them against tank names failed every column they claimed.
- **2026-09-05** — Naming a student on a lost item stamps that they were told, as part of recording it rather than as a second button. An item that names a student and is not marked as notified is a state nobody would mean.
- **2026-09-05** — Invitations expire after 24 hours, not 7 days. Existing pending ones were recomputed from their creation date, so one sent an hour before the deploy keeps the rest of its day and one from last week is expired.
- **2026-09-05** — Resend keeps `requireCanInvite` + `requireOwnKind` rather than being narrowed to owner/admin. The staff screen it lives on is already owner/admin server-side, and narrowing the endpoint would break POOLSE-01's instructor-invites-a-student flow on another screen.
- **2026-09-05** — An expired invitation is rendered as information, never as an error. With a 24-hour window it is the ordinary outcome, and error styling on the ordinary outcome teaches people to ignore error styling.
- **2026-09-05** — A turma requires a tank and a lane count; an instructor stays optional. Refusing an unstaffed turma would make the product unusable in September, which is the month a club sets its timetable up.
- **2026-09-05** — POOLSE-QA-07's "an empty save is still a save" is superseded for turmas. Its point was that an empty form must not 500; the answer is now a 400 naming the two fields, which is the same courtesy by a better route.
- **2026-09-05** — The weekly timetable hides weekdays the site is shut on, except where a class already runs on one. The existing "a disabled weekday keeps its classes" rule wins over the new filter.
- **2026-09-05** — Restoring a cancelled occurrence is owner/admin, narrower than cancelling it. An instructor may call off their own class; putting one back undoes somebody else's decision as often as your own.
- **2026-09-05** — A class cancelled *by a closure* cannot be restored from the toast. Those come back when the closure is lifted, in SQL; the pool being shut is not a fact for one operator to overrule.
- **2026-09-05** — Past days on the calendar are faded and undraggable, not hidden. The register of a class that happened is still worth opening.
- **2026-09-05** — G1 applies to withdrawing a swim time: it is now owner/admin, where it used to allow instructors. The code carried a written argument for the exception and asked for it to be decided rather than swept; this is that decision. The cost is real — an instructor who mistypes a time waits for an admin to withdraw it — and reversing it is one line.
- **2026-09-05** — Medical leave dates are cast to text in SQL. A `date` column parsed into a JS Date at local midnight left the API a day early in UTC, so this was an off-by-one-day bug rather than only a stray time.
- **2026-09-05** — The student fees screen splits into three sibling cards — Plans, Membership, Period total — with the total last, because a total above its parts cannot be checked against them.
- **2026-09-05** — The fee period switcher offers only periods that exist: the current one, plus those already settled. It is hidden entirely where there is no history, since a select with one option is a question with a single answer.
- **2026-09-05** — `shapeOf` gains a `decimal` shape reading both separators, and `looksNumeric` replaces the `/^\d+ digits$/` predicate four importers each wrote. A Portuguese decimal used to shape identically to a word, which made a numeric column check unusable on the default locale.
- **2026-09-05** — `sql:check` runs its own fixtures before every scan. It had silently stopped catching a whole shape of the bug it exists for, and a guard that has never been shown to fail is one nobody should trust.
- **2026-09-05** — The import wizard's state machine is extracted as a hook, not a component. The machine is identical between the register and the store room; the previews are not, and a shared component would have grown a flag for each difference. Calendar and water stay out on purpose.
- **2026-09-05** — The water import moves out of the pool page header and into the Water quality card, under "Record an analysis". Round 5 was right that a text link nobody scrolled to was wrong, and wrong about where it belonged: a page header is about the page, and this is about the readings.
- **2026-09-05** — A water-analysis report (PDF or photograph) is read by a Claude-backed import agent behind `WATER_REPORT_AI_ENABLED` plus `ANTHROPIC_API_KEY`, off by default. Only the document is sent, and every extracted value goes through the spreadsheet importer's own validation and preview before anything is written.
- **2026-09-05** — The report's original file is not stored. File storage stays the deferred decision it was; this would be its fourth caller, and one decision still unblocks all of them.
- **2026-09-05** — The calendar's register and cancel controls move out of the grid block and into the foot of the turma hover card, which is extended rather than duplicated. A block on the lane grid is a rectangle whose height means a duration; there is no room in it for two buttons.
- **2026-09-05** — The app gets one `Dialog`, built on `createPortal` rather than a fifth Radix package. A centred modal is a portal, an Escape key, a backdrop and a focus round trip; the hard parts Radix earns its place for — edge placement, hover intent — are not in it.
- **2026-09-05** — The cancel confirmation is owned once by the board and asked about a session by id, instead of one form mounted per session. Round 5's rendered in place of its own trigger, inside a cell with `overflow-hidden`.
- **2026-09-05** — "Adjust the slot grid" confirms on the calendar before navigating, with the sentence the slot editor already used. The grid and the facility's schedule grid are one table and therefore always in sync; the opening hours are deliberately not rewritten, because the grid is validated to sit inside them.
- **2026-09-05** — A lesson plan is keyed on `(class_group_id, on_date)` rather than on the session row, because sessions are regenerated when a turma moves and a plan must not vanish when somebody changes the pool.
- **2026-09-05** — Writing a lesson plan is owner, admin, the turma's instructor, or a substitute covering that lesson. A substitute counts because they are the person teaching it; with no instructor assigned, only owner and admin, since there is nobody else to name.
- **2026-09-05** — An empty lesson plan is deleted rather than stored blank, and the schema refuses a body of whitespace. A plan that exists and says nothing is worse than none, because a colleague stops looking.
- **2026-09-05** — The plan sheet saves on an explicit button, not on a timer. A plan is prose being composed, and an autosave writes half a sentence to a colleague's screen.
- **2026-09-05** — The Paid control moves down beside "How they pay" and is drawn as a checkbox, while staying a form submit so it works before JavaScript. The two belong together: what the family agreed to, and whether this period of it has arrived.
- **2026-09-05** — `student_fee_payment` gains `source` (`manual` / `mbway` / `sepa`), and `setOccurrencePaid` becomes `markFeePaid` taking it. A webhook settling a month must be distinguishable from a clerk settling it, or a bank reconciliation cannot be done.
- **2026-09-05** — The Period total card shows Total paid and Total remaining as two lines, with the live one in bold. Weight rather than colour, and both always present so nothing is inferred from an absence.
- **2026-09-05** — No disabled Paid control for non-managers: `GET /students/:id/fees` is already owner/admin, so a disabled state would be code no user can reach.

## 2026-09-06 — Management user sizing per tenant

**Decision.** Poolse targets 10–30 management logins per tenant (Owner, Admin,
Instructor, Maintenance). Nothing may degrade up to ~150. Students and
Encarregados de Educação are excluded from this count and scale independently.

**Rules.**
- Exactly one Owner per tenant, enforced. Transfer ownership is the only way to
  change it.
- Seat cap is a soft quota: `organization.max_management_users` (nullable =
  unlimited), checked when an invitation is *created*, not when accepted.
- Pending, unexpired invitations count toward the quota — the 24h expiry window
  would otherwise let a tenant overshoot.
- Staff list needs search + pagination beyond ~30 rows.

**Plan tiers (indicative, not final).** Starter 5 seats / 1 facility · Pro 25 ·
Business 100 · above that, custom.

**Rationale.** A single municipal pool or swim school runs ~1 owner, 1–2 admins,
6–15 instructors, 2–4 maintenance staff. A multi-facility câmara group reaches
40–60. Every management user is a billed Clerk MAU. The quota exists so the
pricing model is enforceable before the second, paying tenant — the first
production tenant is a free pilot and will not test the limit.

**Open.** Whether students and guardians become Clerk users at all when the
mobile app ships, or get a lighter auth path.

**Not built in this pass.** `max_management_users` is not in the schema yet; it
goes in with the next migration that already touches `organization`.

## Round 6 — Espaços, cleaning and maintenance requests

- **2026-09-06** — A facility's non-pool areas are `space`, not `room`. The concept has to hold the car park and the plant room as comfortably as it holds a balneário, and "room" quietly excludes both.
- **2026-09-06** — `maintenance_request` is built as the seed of Módulo 2 rather than as a spaces-only feature: keyed on a facility, with nullable `space_id`, `pool_id` and `inventory_item_id`. Equipment and tank faults will extend this table instead of arriving with one of their own, because two places to look for "what is broken at this site" is the failure the module exists to prevent.
- **2026-09-06** — Overdue is derived at query time, in SQL, in one place. No `is_overdue` column, no cron job, no worker — a stored flag needs a process to keep it true, and per-tenant running cost is a design constraint. The API ships the boolean and the client renders it rather than re-deriving it.
- **2026-09-06** — An archived cleaning log did not happen: every last-cleaned read filters it out, so deleting a mistaken entry puts the space back to overdue. The alternative leaves a dirty room looking clean because somebody corrected a mistake.
- **2026-09-06** — A space with a cleaning interval and no cleaning at all is overdue, not blank. An absence of history is not evidence of cleanliness, and it is exactly the state the feature exists to surface.
- **2026-09-06** — `space.active = false` means out of service and is exempt from the overdue rule; `archived_at` remains deletion. Two flags earn their place only by meaning different things, and the exemption is what gives `active` a job — nobody cleans a balneário shut for building works.
- **2026-09-06** — Enum values stay English snake_case and the Portuguese is an i18n key. `restock`, not `reposicao`: that word is already the make-up-lesson module's, and one word meaning two unrelated things in one schema is how somebody joins the wrong table at midnight.
- **2026-09-06** — Logging a cleaning and reporting an issue are open to every management login, including instructors; resolving is owner, admin or maintenance. Reporting is noticing, resolving is a judgement that the work was done — and a feature that made an instructor find an admin to record that they mopped the balneário would simply not be used.
- **2026-09-06** — A cleaning log has no edit control at all. An entry is a claim about a moment; correcting one means archiving it, which is owner/admin.
- **2026-09-06** — Resolving an already-resolved issue is a 409, not a silent second write. Two people closing the same fault from two phones must not quietly rewrite who fixed it and when.
- **2026-09-06** — A space's type is optional and defaults to `other`, matching the column default; only a *stated* invalid type is refused. A club naming a room the six categories do not cover must not be stopped by the classification.
- **2026-09-06** — The free-text inventory locations became spaces, matched case- and accent-insensitively per facility, named with the spelling on the earliest-created item. Only non-archived items mint a space; archived ones are linked where one already exists. `inventory_item.location` stays in place — dropping it is a separate change so the result can be eyeballed first.
- **2026-09-06** — The API answers `canManage`, `canLog` and `canResolve` on the read, from the same helper the guards use. The screen shows a control only where pressing it would work, and the client never derives a permission from a role list.
- **2026-09-06** — Espaços appears on both Instalações (inside each site's card, under that site's tanks) and the site's own page, from one component with a `variant`. Asked for on the list screen first and then on both; a second copy of the panel is how two screens start disagreeing about what a space is.
- **2026-09-06** — Instalações fetches one space list per site in parallel rather than folding them into `/facilities`. A loop over an endpoint is usually wrong; a licence bounds a club to one or two sites, and the alternative puts every site's cleaning state behind the request that draws the page and loses the property that a failed block costs only itself.

## Round 6 — the calendar's week grid

- **2026-09-06** — The calendar gets its own grid; the Turmas screen keeps `schedule-board.tsx`. They were one 4,166-line component told apart by whether a `weekStart` was passed, so every change to the dated view was a change to the recurring one. Two grids overlap for now; folding Turmas onto the new one is proposed, not done.
- **2026-09-06** — The axis stays a week: seven days, each subdivided by pista, with time down the left on a pixel scale. A single-day view with lanes as columns was the other candidate and would have been closer to Google Calendar; keeping the week is what the screen is for.
- **2026-09-06** — Blocks are placed from their minutes at one pixel per minute, replacing slot-as-table-row. A 45-minute class in a 60-minute slot used to fill the row and read as an hour, which is a lie the operator had to know to discount.
- **2026-09-06** — A drop asks *this week / every week* in a popover at the cursor rather than a centred modal, and the block has already moved by the time it asks. Round 5's decision that the board must ask rather than guess stands; only the staging changed.
- **2026-09-06** — Snapping is to the facility's own slot rows, not to a fixed increment, falling back to the greatest common divisor of the grid's own times where no row covers the moment. There is no stored increment, and a real timetable is a list of rows rather than a number.
- **2026-09-06** — One droppable per day-and-lane column instead of one per cell. Measured on the reference club: 2,256 droppables became 168, a 13.4× cut, and that count — each cell running a rules evaluation on drag start — was the clunkiness, not the drag library.
- **2026-09-06** — Event colour is the level, in the club's own level order, over eight solid tints. Washed tints at 15% measured ΔE 4.5 apart, close enough that the colour carried no information; solid ones are ΔE 26.7 apart with white text at 4.6:1 or better in both themes.
- **2026-09-06** — A parceria keeps its partner's colour, and a turma with no level takes the neutral rather than the first tint. "Not said" must not look like "Iniciados".
- **2026-09-06** — The hover card is removed from the tree during a drag rather than given `pointer-events: none`. The ticket asked for the latter; it would have disabled Take the register and Cancel class, which are buttons inside the card that round 6 deliberately moved there.
- **2026-09-06** — Clicking an empty cell places an existing unscheduled turma rather than opening the new-turma form. That form has no day or time fields on purpose — a turma with no days is a valid half-finished thing — so the day and time a click carries would have gone nowhere. The lane is still not set by the click, because `placeSlotAction` returns no schedule id; dragging the block one column is the answer until it does.
- **2026-09-06** — The calendar gets a wider content column (`max-w-page-wide`, 104rem) and is the only page allowed one. AC5's rejection of per-page widths holds for pages made of text; it was never about a screen whose content is a grid, where the cap protects nobody and costs half the monitor.
- **2026-09-06** — The calendar draws only the weekdays the site opens on, from the facility's opening hours — keeping round 5's exception that a closed day still carrying a class is drawn anyway, shaded. A class that is invisible is a class nobody can move.
- **2026-09-06** — Classes on a holiday, on a day the pool is shut, or on a day already past are all faded, and **all three stay draggable**. This reverses the round-5 rule that past classes are undraggable: in use it read as a broken grid, since the block looks like every other block and does not move. Rearranging last Monday from Wednesday is ordinary planning, and the server already refuses what is genuinely impossible.
- **2026-09-06** — The lane header names the row "Pistas" once and numbers the columns by each lane's position in its pool, rather than repeating the lane's name on every column. Position rather than digits parsed out of the name: a club with "Raia A" and "Central" has no digits to parse, and a mix would put a column headed 4 in third place. The name stays on hover and in the hover card.
- **2026-09-06** — The calendar card no longer scrolls vertically: it is as tall as the day and the page scrolls. The day and lane headers scroll away as a result, which was accepted knowingly — `overflow-x: auto` forces `overflow-y` to auto, so a container that scrolls sideways cannot also stick to the viewport vertically. The time gutter keeps its sideways stickiness, which is the axis that still scrolls.
- **2026-09-06** — A block's left and right edges resize its lane span. The change applies to every week and the confirmation offers one button saying so, rather than a two-way choice whose answers would both do the same thing: lanes live on the recurring booking and `moveOccurrenceAction` carries only a start time.
- **2026-09-06** — Lane columns narrowed again, 92px to 64px. A six-lane pool now fits a week across a laptop without sideways scrolling, which is worth more than the tail of a truncated turma name — the full name is on the hover card.

## Round 6 — colour, and the calendar's second pass

- **2026-09-06** — A turma can carry its own colour, stored as a `class_colour` enum of eight token names rather than a hex. The same three reasons `booking_category.colour` gave: colour comes from tokens, a hex is one colour for two themes, and each of the eight was measured for contrast. A free picker lets a club take its own block text to 1.3:1 with no warning. `partner.colour` stays a hex; it predates this and a slice about turmas is not where that gets changed.
- **2026-09-06** — A turma with no colour falls back to its level's tint rather than going neutral, so nothing changes for a club until somebody picks something.
- **2026-09-06** — The calendar's hover card closes after 220ms, not immediately. "Closes immediately on pointer-out" was taken literally from the ticket and made the card useless: Marcar presenças and Cancelar aula are inside it, and the pointer has to cross the gap to reach them. The delay is travel time.
- **2026-09-06** — Take the register and Cancel class also appear at the foot of the lesson-plan sheet. Clicking a class is how most people arrive; being told to close it and hover the block instead is how a feature goes unused.
- **2026-09-06** — The move/resize confirmation is anchored to the block it changed, with a tail pointing at it, and flips above the block when it would otherwise open off the bottom of the window.
- **2026-09-06** — The Espaços section starts collapsed on both screens, with counts of spaces, overdue cleanings and open issues in its header. A shut section is only safe if what it is hiding is still legible.
- **2026-09-06** — An open issue on a space is marked in amber with its own icon, distinct from the red of overdue cleaning. A spotless space with a broken shower is not the same problem.
- **2026-09-06** — `SpacesPanel` takes where "back" should go, instead of assuming the site's page. Opening a space from Instalações and pressing Voltar used to land somewhere the operator had never been.
- **2026-09-06** — `classes.colours.magenta` is listed in `SAME_ON_PURPOSE`: it is the same word in both languages. Renaming the pt-PT one to "fúcsia" to satisfy the guard would be choosing a different colour to dodge a check.

## Round 6 — colours, stand-ins and leave

- **2026-09-06** — A level carries its own colour, backfilled from the position the calendar was deriving it from. Nothing changed visually on the day, and the derived rule was deleted rather than left as a fallback nobody would remember. `class_colour` is reused rather than duplicated: a level's green and a turma's green must be the same green.
- **2026-09-06** — On the Classes screen a turma's left rule is its own colour, falling back to its level's, so a turma looks the same there as on the calendar without anybody colouring anything.
- **2026-09-06** — Changing the teacher on the calendar is a stand-in for that lesson only, written to `class_session.substitute_instructor_membership_id`. Reassigning the turma stays on the turma's own screen. Next week goes back to normal by itself, which is what a stand-in means.
- **2026-09-06** — An instructor on approved leave is shown in the stand-in list, greyed, with the reason, and cannot be chosen; the endpoint refuses them again. Hiding them would answer "where is Ana?" with silence, and "on holiday" and "left the club" are different things.
- **2026-09-06** — Availability is read from approved leave only. A request nobody has answered is not an absence, and treating it as one would let an unanswered form veto the timetable.
- **2026-09-06** — Medical and personal leave join `vacation_request` as a `kind` rather than getting a table of their own — one approval queue, one thing for the calendar to ask. Only `vacation` counts against the yearly entitlement: a week of flu is not a week of holiday.
- **2026-09-06** — The staff list shows who is away *today*, from approved leave, with the kind in words. Only today, because that list answers "who is here" rather than "who has anything booked this year".
- **2026-09-06** — The calendar's day boundaries are a 2px `border-strong` rule against the lanes' hairlines. At 64px columns the week read as one undifferentiated ladder and finding Thursday meant counting.
- **2026-09-06** — The first hour label sits just below its line rather than centred on it. Centred put half of it above the canvas, where the header covered it — and the club's opening hour is the time somebody is most likely to be looking for.
- **2026-09-06** — The lesson-plan sheet is `max-w-2xl` rather than the dialog's usual `max-w-md`. A plan is prose being composed, and at 28rem a line of sets wrapped twice.
- **2026-09-06** — The slot-grid builder is removed from the facility's Opening hours card; the slot rows stay and keep driving the calendar's snapping, the Turmas board, the printed sheet and the XLSX export. What the calendar made pointless was arranging those rows by hand, not the rows.
- **2026-09-06** — The calendar's legend became one card that is also the filter: a checkbox, a swatch and a count per level, plus parcerias, eventos and manutenção, laid out in columns like the vacations team map. A legend that tells you what a colour means without letting you act on it is a longer route to the same place.
- **2026-09-06** — A dragged block shows the time it would land at rather than the time it came from. The block is under the pointer and the gutter is a week away on the left, so reading the landing time off the ruler means looking away from the thing being moved.
- **2026-09-06** — Today's column keeps a faint tint and pulses once for two seconds when the week is opened, dropped under `prefers-reduced-motion`. A column that goes on blinking cannot be read.
- **2026-09-06** — `DAY_RULE` ties the block layer's arithmetic to the day separator's width. They disagreed once — the rule went to 2px and the maths kept adding 1 — which drifted every block up to seven pixels by Sunday and made a drag near a lane edge land one lane over from where it looked.
- **2026-09-06** — A refused move shows the server's own sentence alongside the generic key. Refusals come from several different rules and folding them into "it did not work" produces reports nobody can act on.
- **2026-09-07** — A one-week move carries its pistas, reversing the 2026-09-06 decision that a lane change applies to every week. That decision was honest about a limitation rather than a preference: `moveOccurrenceAction` carried a start time and nothing else, so "só esta semana" could not express a lane. A session has its own rows in `class_session_lane`, so it can — and "pista 3 is shut for repairs this Tuesday" is a real thing a club needs to say. The scope popover therefore offers both answers for a sideways change as it does for a move in time.
- **2026-09-07** — The tank selector moves to the left of the calendar toolbar and gets a primary border. It decides which pistas the whole week is drawn from, and it was sitting on the right at the same visual weight as "Ver todos" — the page's most consequential control dressed as a tidy-up button.
- **2026-09-07** — The seed's leave fixtures gain a second kind: somebody away *today*, anchored to the facility's own date rather than a fixed one. The three demo states are a week next April, which is right for the vacation map and invisible on the staff list — so the away chip could only ever be checked by editing the database by hand.
- **2026-09-07** — A class whose register has been taken cannot be moved, in either scope. One mark means somebody stood at the poolside with a list, so the day, hour and pista are a record rather than a plan. This does *not* re-open round 6's reversal about past classes: the block stays draggable, because one that looks like every other block and silently refuses reads as a broken grid. The server refuses and says why, and the hover card shows "Presenças: já marcadas" beforehand.
- **2026-09-07** — The calendar's horizontal scale moves to `lib/calendar-scale.ts` beside the vertical one, and a drag resolves its lane from travel rather than from the column under the pointer. Keeping one axis in the component and one in the library is what let them be answered by different questions: the time from how far the pointer had moved, the lane from what it was over. For a block wider than one pista those give different answers, and the block landed on lanes nobody had pointed at.
- **2026-09-07** — `<body>` carries `suppressHydrationWarning` alongside `<html>`. Browser extensions write attributes onto it before React hydrates, and the resulting console error is noise a real hydration bug can hide behind.
- **2026-09-07** — Named date formats (`long`, `short`, `stamp`) are defined once in `i18n.ts` and handed to both `getRequestConfig` and `NextIntlClientProvider`. next-intl has no built-in named formats, so asking for one that was never configured throws `MISSING_FORMAT` at render time — which broke the calendar for every turma while partnership bookings, having no plan panel to open, went on working. `pnpm i18n:check` now proves every named format resolves, because a format name is a string and `tsc` has no opinion about it.
- **2026-09-07** — The calendar keys a week's per-occurrence controls by the **booking's id**, not by `turma|weekday|hora`. The composite was exact only while an occurrence could not leave its pattern's slot; a one-week move carries a day, an hour and a set of pistas, so the session sits at one slot while the block is still drawn at the pattern's. The lookup then matched nothing and the register link, the cancel button, the teacher picker and the lesson plan silently vanished from precisely the classes somebody had rearranged. `slotKey` stays as the fallback for a session with no booking behind it.
- **2026-09-07** — The calendar draws each booking **where its session for that week actually is**, overlaying `class_session` onto the pattern `readGrid` returns. Without it a one-week move was written correctly and then invisible — the grid reads `class_schedule` only, so the block sprang back to the pattern's slot on the next refresh, which is indistinguishable from a drag that did not work. Overlaid on the page rather than in `readGrid` because the week's sessions are already loaded there and the grid endpoint takes no date range. A booking with no session that week stays where the pattern puts it.
- **2026-09-07** — A click on a block is ignored while a drag or a resize is live, and for 250ms after either ends. The hover card is suppressed for a resize as well as a drag. Opening the lesson plan on the tail of a gesture put a sheet over the grid in the middle of moving something.
- **2026-09-07** — The lesson plan sheet opens with the class it belongs to — its name and the same facts the hover card carries — and the teacher picker sits **above** Save. The picker writes on change; under the button it read as something Save would record, so a stand-in chosen and not "saved" looked lost.
- **2026-09-07** — The training plan is reached from a **button on the hover card**, and a block is no longer clickable at all. A block on this grid is a thing you drag, and every drag and resize ends with the browser firing a click underneath it; guarding that with a 250ms stamp worked and still left the sheet one twitchy pointer away from opening mid-move. The button sits beside Take the register and Cancel, which is where somebody already looks for what they can do with a class — and unlike a click on a block, the card opens on keyboard focus, so it is reachable without a mouse.
- **2026-09-07** — The lesson plan sheet is a flex column that fits the window rather than a stack that scrolls. The text box takes whatever height is left (`flex-1` with `min-h-0`) instead of a 22rem floor, which had pushed Save, the teacher picker and the two buttons below the fold on a laptop before a word was typed. A long plan scrolls inside the box, which is what a text box is for; the panel keeps `overflow-y-auto` underneath as the fallback for a genuinely short window.
- **2026-09-07** — The sheet's foot shows the register and cancel buttons only: not the teacher picker, which is above Save, and not the plan button, which would lead into the sheet already being read. `sessionButtons(booking, withPlan)` builds the row once and each place takes the part of it that makes sense there.
- **2026-09-07** — A partnership can be one the club runs the lessons for: `partner.managed_lessons`, one switch on the partnership detail screen, off by default. Its calendar blocks then carry a training plan and Cancelar aula, exactly like a turma's. **Not a register** — a `partner_group` holds a `participant_count` and no people, and `attendance` needs a real `student_id`, so "marcar presenças" would have to mean a headcount, which is a different feature with its own table. Off by default is the compatibility story: every parceria that existed goes on behaving as POOLSE-46 settled until somebody turns it on.
- **2026-09-07** — `lesson_plan` may belong to a `partner_group` instead of a `class_group`, with a CHECK that exactly one is set and a partial unique index each. A second table would have needed its own read path, its own permissions and its own copy of every bug; a partner group is the same kind of thing for this purpose — a named set of people who turn up at an hour.
- **2026-09-07** — The calendar block shows the person teaching it *that week*: a stand-in overrides the booking's instructor on the block, and the hover card labels it "Professor desta aula" so a cover is not read as a permanent reassignment. Same reasoning as the hour — a dated week shows what is true that week.
- **2026-09-07** — `listSessions` left-joins `class_group` instead of inner-joining it, so a partnership's sessions reach the calendar at all. The inner join had silently dropped every one of them since the calendar was built; nobody noticed because a parceria had no per-occurrence controls to miss. `managed_lessons` made the omission visible: the switch saved, the grid carried it, and the controls came back empty because the sessions they hang off had never been in the list. A partnership session takes its name from its partner group, its instructor from its booking, and its headcount from `participant_count`.
- **2026-09-07** — Every save says so, through one toast raised in `useSavedAction`. The hook already compares state identity to decide whether to refresh the router, and that is the same moment a message belongs — so 82 call sites get feedback without any form remembering to ask for it. Success ~4s, warning and failure ~8s, an × on every one, top-centre above the dialog layer.
- **2026-09-07** — The toast is the surface colour with a tinted border and icon, not a saturated fill. A success that floods the top of the screen is a celebration of something routine, and these appear dozens of times an afternoon. Tone is never colour alone: each has its own icon and the words say what happened.
- **2026-09-07** — Field-level errors stay beside their field; only the duplicated top-of-form banner and the inline "Guardado" move to the toast. A message at the top cannot say which of a dozen boxes it meant, so the marker on the field is the useful half. 33 inline error lines and 6 inline success lines were removed, all in files that go through `useSavedAction`.

## Round 8 — the partnerships importer gets its interface, and the list comes back out

- **2026-09-07** — POOLSE-48's remaining half is built: the four-step wizard on the Parcerias panel and the partner list as `.xlsx`/`.csv`. The API half had shipped on 2026-09-04 with 32 tests and no way for anybody to reach it.
- **2026-09-07** — The preview is a **tree** — partnerships as headings with their groups beneath — rather than the flat table every other importer draws. A school's sheet repeats its name down the column, so twelve rows across three schools is three partnerships with twelve groups; drawn flat, eleven of those rows would say "this partnership already exists" and read as eleven problems. The button counts both, so "3 parcerias e 12 grupos" is what lands.
- **2026-09-07** — A refused row is shown in a section of its own rather than greyed in place. It belongs to no partnership — the API leaves it out of the tree precisely because it cannot be committed — so there is no heading to put it under, and inventing one would imply a partnership the commit will not create.
- **2026-09-07** — The wizard reuses `useImportWizard` and expresses "already recorded" through the `duplicate` field the hook asks each importer for, aliased to `groupId` rather than recomputed. The hook's default tick and the API's own default (`include === null` means "create what is new") then cannot drift apart, which is the failure the alias exists to prevent.
- **2026-09-07** — The partner export writes the **enum's own spelling** into the Tipo column — `jardim_infancia`, not "Jardim de infância" — for the reason the inventory export writes `all` rather than a translated "todas": a file exported under `en` has to re-import under `pt-PT`. The importer goes on reading the human words in both languages, so a column a person typed is unaffected.
- **2026-09-07** — `readPartnerType` now reads the enum's own spelling before the human vocabulary. It did not, and two of the eight values are a pair of words joined by an underscore that no entry in that vocabulary contains — so a jardim de infância exported and re-imported came back as `outro` with an "unknown type" warning. The round trip was quietly downgrading the one type the reference seed uses most. Guarded by a test over the whole enum rather than over the two that were broken, because a ninth value would have the same problem and the failure is a warning nobody reads twice.
- **2026-09-07** — A partnership with no groups still gets a line in the export, with the group cell empty. Skipping it would drop a real partnership out of its own export and an operator who exported, edited and re-imported would silently lose it; as a blank it comes back as one refused row naming the missing group. A disappearance is worse than a question.
- **2026-09-07** — The export carries **one contact**, the first by name. The sheet has one set of contact columns, and a partnership with three contacts cannot be flattened into them without inventing a rule about which one matters. A round trip neither duplicates that one nor deletes the other two, because the importer only ever adds a contact it can reach.
- **2026-09-07** — `partners.field.contactName` is "Contacto", not the partner screens' own "Nome". On a partnerships sheet a bare "Nome" belongs to the entity — `partnerName` claims it, which is what makes a one-name-column sheet importable at all — so a contact labelled "Nome" would have handed the entity's column to the contact on every file Poolse exported. Held still by a test.
- **2026-09-07** — The drop confirmation uses `components/ui/dialog.tsx` rather than the hand-rolled fixed overlay the register's and the calendar's importers still carry. Escape, the backdrop, the focus round trip and the focus trap come with it; those two can adopt it whenever somebody is in there anyway.

## Round 8 — a class with no pista is drawn

- **2026-09-07** — A booking with no lanes is drawn in a **Sem pista** column, first in every day, instead of not being drawn at all. Nine of the dev tenant's bookings were in that state: they existed, nothing refused them, and the calendar showed none of them. Known and left alone since round 7 because it needed a call on which column such a block belongs in.
- **2026-09-07** — The column is a **synthetic lane prepended to the day**, not a special case in the geometry. `columnX(day, laneIndex, laneCount)` is one uniform stride per day and the blocks, the ghost and both axes of the drag all measure with it; a column present on some days and not others would make the stride depend on the day, which is precisely the shape of bug rounds 6 and 7 were spent removing. So it is on every day, and appears at all only when the week contains a class with no pista.
- **2026-09-07** — The column is **display-only**. Dragging a block *out* of it onto a real pista assigns that pista — the reason for drawing it. Dragging *into* it does nothing: the drop floors at the first real pista, sideways resize is refused for a class with no run of lanes, the column takes no click, and the synthetic id is filtered out of every lane list before it is written. Removing a class's pistas by dragging is an easy accident with no undo, and the move endpoint already has a deliberate way to say "in no pista this week".
- **2026-09-07** — The column is headed `s/ pista` rather than a number, and a note above the grid says in full what it is and how many classes are in it. A 64px column cannot carry "Sem pista atribuída", and an abbreviation whose only expansion is a tooltip would be information living in a tooltip — which the standing rule forbids. A pista's number is now its place among the *real* pistas, so the first pista is still headed 1 with the column present.

## Round 8 — a refusal says where the thing in the way actually is

- **2026-09-07** — A `laneTaken` refusal now carries the **weekday and hour of the pattern it is defending**, and the message names them: "Pista 2 · Hidro ginastica bebés · na marcação semanal de Quarta, 10:45". Reported as a partnership block that would not move, citing a conflict with a class nowhere to be seen. Both halves were correct: a series move is checked against the recurring booking, because that is what a series move rewrites; the calendar draws each week's *session*. A turma whose four sessions had all been moved to Saturdays one week at a time still held its Wednesday pattern slot, so the guard defended a slot the screen was not drawing. The guard is unchanged — only the sentence, which was true and impossible to act on.
- **2026-09-07** — Not fixed by making the pattern stop blocking once its sessions have moved away. That would have made this refusal disappear, and would also let two classes collide the moment somebody reset one week back to its pattern — a rule that depends on generated data rather than on the timetable.
- **2026-09-07** — The calendar's refusal moves from a red bar above the grid into the toast. On a calendar as tall as the pool day, "above the grid" is off the top of the screen by the time somebody has dragged a block to Thursday afternoon, so the one message that has to be read was in the one place nobody was looking. It also gets 8s rather than being dismissed by the next render.
- **2026-09-07** — A toast is `w-auto` up to `max-w-md` rather than always `w-full max-w-md`: the box hugs its words. "Guardado" in a 28rem box with 24rem of empty surface beside it made every message look the same size whatever it said. The ceiling stays so a long refusal wraps into a readable column instead of running the width of a monitor.
- **2026-09-07** — The saturated OutSystems fill was offered and declined: the toast keeps round 7's muted surface with a tinted border and icon. These appear dozens of times an afternoon and a flood of colour for a routine save is too loud.

## Round 8 — a series move carries its weeks

- **2026-09-07** — **A move made "todas as semanas" now re-times the booking's own sessions.** It did not, and that was the bug behind "every week doesn't seem to work, and no feedback is given": the calendar draws each booking where its `class_session` for that week actually is — round 7's overlay, which is what made one-week moves visible at all — and a series move rewrote only `class_schedule`. The block sprang back to the session's old slot on the next render, with no error, because nothing had failed. Proved against the live dev data: the API answered `{moved: true}` and the row had genuinely changed.
- **2026-09-07** — The same drift is what produced refusals naming classes nobody could see. A booking whose pattern says Wednesday 14:15 while every session sits at 10:15 still defends 14:15 against everyone else. Naming the pattern slot in the message (earlier today) treated the symptom; this is the cause. Both fixes stay — a pattern slot can still legitimately differ from a week that was moved by hand.
- **2026-09-07** — Three kinds of week deliberately do **not** follow: one moved by hand (`moved_at IS NOT NULL`), because that is what "só esta semana" means and what the column exists to record; one in the past, because a series change is a change going forward; and one whose register has been taken, because a taught class is a record. Today is read in the facility's own timezone, not the server's.
- **2026-09-07** — A week that cannot follow — its new hour is already taken that week — is **left where it is and counted**, rather than failing the whole move. Each week is re-timed inside its own savepoint. Refusing an entire series change because one Wednesday in November already has something in it would make the feature unusable on any club that has ever moved a single class; the count comes back as `weeksBlocked` and the grid says it as a warning, because silence would be the drift this fix exists to stop, reintroduced quietly.
- **2026-09-07** — Re-timing a session re-stamps its `class_session_lane` rows. The exclusion constraint is on those rows and they carry their own copy of the window, so a session moved without them would leave the database defending an hour the class no longer runs at.
- **2026-09-07** — **The Sem pista column is now a drop target both ways**, reversing this afternoon's display-only decision at Rui's request after using it. Dropping a block there clears its pistas; dragging it out onto a pista assigns one. The original objection — an accidental drag silently unassigning pistas — is answered by the scope popover, which every move already goes through, so no such change is silent. Landing on the column means *no pista* whatever the block's width, rather than slicing the span and filtering: a three-lane block dropped half over the column would otherwise become a two-lane block, which is a data change nobody asked for.
- **2026-09-07** — The column takes a drop but not a click. Creating a class there would make another class with no pista, and the whole reason the column exists is that those are hard to find. `LaneColumn` therefore has `creatable` separately from `disabled`; one flag for both would have meant choosing which of the two to get wrong.
- **2026-09-07** — "Já existe" now says the rule reaches across every tank: a turma or partner group cannot be at the same weekday and hour twice, in any pool, because it cannot be in two places at once. Reported as the calendar conflating pools; the rule is right and the message simply never said what it was.

## Round 8 — the week you dragged is not an exception to your own gesture

- **2026-09-07** — **A series move carries the week that was dragged, even if that week had been moved by hand before.** Reported as "the every week set seems to not work, nothing happens, no feedback whatsoever" — and it was not the endpoint: the API answered `{moved: true, weeksFollowed: 3}` and three later weeks did move. The week on screen was `moved_at IS NOT NULL`, because an afternoon of testing had moved almost every visible block one week at a time, so the one block being watched stayed exactly where it was picked up. The rule "a hand-moved week stays" is right for *other* weeks; applied to the week whose block the operator has this second dragged, it contradicts the gesture that asked for the change. The move now carries `fromDate`, and that week follows and loses its exception.
- **2026-09-07** — **Weeks kept back are counted and said out loud** (`weeksKept`), as blocked ones already were. A hand-moved week staying put is correct and *invisible*, and invisible is what made a working feature read as broken: the symptom of "kept by design" and "failed" is identical — a block that does not move. Only one message is shown per move, blocked taking precedence, rather than two about the same change.
- **2026-09-07** — `fromDate` is optional and absent means "no particular week", so every existing caller behaves exactly as before. The grid derives it from the week on screen and the booking's own weekday.

## Round 8 — a single week can be longer, too

- **2026-09-07** — **A one-week move now carries a length.** `moveOccurrence` took a date, a start time and (since round 7) a set of pistas, and never a duration — so dragging a block's bottom edge and answering "só esta semana" wrote the hour and silently dropped the resize. The block sprang back to its old height and the gesture looked like it had done nothing, while the same drag answered "todas as semanas" worked, because the series path has taken a duration since POOLSE-50. Reported as "resizing works for every week but not for this week".
- **2026-09-07** — Absent means "I did not ask about the length", the same contract `laneIds` has: the week keeps the duration it already had. Collapsing that into a default would let a plain time change quietly reset a week somebody had deliberately made longer — the same bug the lanes contract exists to prevent.
- **2026-09-07** — `ends_at` is still not written by the move. A BEFORE trigger derives it from the start and the duration, which keeps that sum in one place; the lane exclusion is on a window built from those two, so a duration written without it would leave the database guarding an hour nobody is swimming in. Asserted in the tests rather than assumed.
- **2026-09-07** — ~~Known and not fixed~~ **— fixed on 2026-09-08, see round 9 below. A one-week move updates `starts_at` but leaves `occurs_on` on the old date.** Several sessions in the dev database now have the two disagreeing — one with `occurs_on` on a Saturday whose clock says Wednesday. Nothing reported today traces to it, because the calendar reads `starts_at`; it would bite a move that crosses a week boundary (Sunday to Monday), where the session would be filed under the wrong week, and `retimeSessions` derives a session's week from `occurs_on`. Left alone deliberately at the end of a long session rather than changing a lookup that is now working; it is the first candidate for the next one. **It was: `retimeSessions` was asking an on-screen question of the pattern anchor, and the diagnosis here was right about where it would bite.**

## Round 9 — the drift between where a class is filed and where it is drawn

- **2026-09-08** — **The `occurs_on` drift is fixed where it actually bit: `retimeSessions` asked an on-screen question of the pattern.** Logged yesterday as known and not fixed. `occurs_on` is the day the pattern implied and a one-week move deliberately leaves it alone — that is what stops the next regeneration putting a second class back on the old day, and the test that proves it still passes. So a class moved across a week boundary sits in one ISO week under an `occurs_on` belonging to another, and the function was answering *two* questions from that column. Only one of them was its business.
- **2026-09-08** — **Where a session lands, and whether its week is behind us, stay derived from `occurs_on`.** That is what keeps one session per pattern week, which is the invariant the unique index `(schedule_id, occurs_on)` is built on. Deriving the target from where the session actually is would let two sessions of one booking land on the same date the moment one had slipped into the other's week, and turn a legitimate series move into a blocked week for a reason nobody could see.
- **2026-09-08** — **Which block the operator dragged is derived from where it is drawn** — `session_local_date(...)` matched against the `fromDate` the grid already sends, which is the day the block was sitting on. Matched by ISO week, a slipped block was not recognised as the dragged one and kept its exception, so it stayed exactly where it was picked up while three later weeks moved: round 8's silent "todas as semanas does nothing", still there for any class that had crossed a week boundary. The week match was also *over*-inclusive — with a slipped block on screen it marked the neighbouring week as dragged instead, so the wrong session lost its exception. One line, two defects, one test each.
- **2026-09-08** — Not fixed by making a one-week move rewrite `occurs_on`. It reads as the obvious repair and it is the one thing the column exists to prevent: the pattern's original day would come free and the next regeneration would quietly add a second class there. `regenerating does not put the moved class back on its old day` holds that still.

## Round 9 — four kinds of fee, before there is anything to rewrite

- **2026-09-08** — **Fee kinds land before invoicing (2.2), deliberately and in that order.** An invoice line will carry the kind it was raised from, so adding a kind afterwards means rewriting lines that have already been sent to families. This is the cheap end of that change and the reason it was taken out of order.
- **2026-09-08** — **`fee_kind` is `mensalidade | inscricao | seguro | quota`, on one price list.** No parallel table per fee type: all four are "a named amount this facility charges", and four tables would be four read paths, four permission checks and four copies of every bug. `fee_plan` already varied its shape by kind; this widens the set and says in a CHECK what each new shape is — only a mensalidade is priced by a level and a frequency, only a quota is banded by age, and inscrição and seguro belong to a season.
- **2026-09-08** — A new enum type rather than `ALTER TYPE … ADD VALUE`. Postgres will add a value inside a transaction but will not let the same transaction *use* it, and this migration uses both new values immediately in checks and partial indexes. A migration that cannot run in one transaction is one that can half-apply. `fee_plan_kind` stays in the schema with nothing using it, so the Down section has a type to put the column back into rather than recreating one that might differ by a value or an order.
- **2026-09-08** — **The recurrence belongs to the plan, not to the kind.** A club is free to make its quota annual or its inscrição recurring, so the default per kind is an API opinion (`inscricao` → one-off, `seguro` → annual) and the schema enforces only the pair that cannot both be true: a `fee_period` may be named only by a plan that recurs by the facility's periodicity list. Every plan that existed is `periodicity`, which is what `fee_period` already meant, so the student page behaves exactly as it did.
- **2026-09-08** — **Quota folds into `fee_kind` and `student.is_socio` is untouched.** Being a sócio is a fact about a person, and the code has said so since round 5: a member with a waived quota is an ordinary case that "has a quota line" cannot express. The price list is what changed; the student page did not.
- **2026-09-08** — **VAT arrives on `fee_plan`, reversing the decision written into the column's own comment.** That comment argued a family's mensalidade is advertised gross, Art. 9.º CIVA exempts most sports tuition, and a rate nobody maintains is a rate something eventually trusts. Seguro is the case it does not cover: it is normally isento and an invoice has to *say* so, which a schema with nowhere to record it cannot. Amounts stay gross — the rate describes what the amount already contains and no total changes.
- **2026-09-08** — **Isento is its own flag, not a rate of zero.** On a Portuguese invoice an exemption and a zero rate are two different statements, and collapsing them makes the two indistinguishable at exactly the moment invoicing needs to tell them apart. A CHECK stops a row claiming both. The exemption *reason* is invoicing's to add; recording which lines are exempt is what stops that being a retrofit. Every existing plan was marked exempt, which is what "no VAT rate here" already meant.
- **2026-09-08** — **The season entity is not moved to the facility.** It already exists exactly as the brief describes it — name, both dates, one published at a time, `draft`/`published`/`archived`, its own screen under Turmas — and it is wired into `generate_sessions`, `class_group`, `class_schedule` and `facility_time_slot` at organization level. Moving it would re-key the season generator for no gain a licence-per-facility product can use. What the price list does is *pick from* the club's list, never keep a second one; archived seasons are left out, because a year that has ended cannot be priced afresh.
- **2026-09-08** — **`is_renewal` is in the inscrição unique key rather than excluded from it.** That one choice is what lets a season hold a joining price and a cheaper renovação beside it while refusing a third of either. A club that charges everybody the same writes one row and nothing about it has to change to add the second.
- **2026-09-08** — **The apólice is its own entity, and its cost is not the family's price.** `insurance_policy.cost_per_person_cents` is what the club pays its insurer; the seguro `fee_plan` is what a family pays. Usually the same number and never the same fact — a club adding a euro of admin to it would have nowhere to put the difference if one row were both.
- **2026-09-08** — An apólice that still covers students is **not archived**, and the refusal carries the count. Archiving it would leave cover lines pointing at a policy no screen lists, which reads on the student page as insurance that came from nowhere. The number travels as a field and the sentence is composed where the locale is, like every other refusal that needs figures.
- **2026-09-08** — The renewal warning is **sixty days**, a constant rather than a setting, and both it and the expiry are derived on the server against the database's own date. A club that wants longer has not asked; a number nobody has tuned is better than a settings row nobody fills. A policy that looked live on one laptop and lapsed on another would be a support call nobody could reproduce.
- **2026-09-08** — **`student_fee.kind` is snapshotted onto the line**, and the load-bearing reason is not the snapshot convention: a partial unique index cannot join, so "one inscrição per student per season" is only sayable with the kind on that row. A BEFORE trigger fills it from the plan when a caller does not send it — every caller predates the column, exactly as with `class_session.occurs_on` — and a constraint trigger refuses one that disagrees with its plan.
- **2026-09-08** — A 409 from the price list now names **which** control is taken — the level, the age band or the season — rather than arriving as "esse nome já existe". The server action was checking the status before the fields, which is right for a periodicity, whose name is what collides, and wrong for all four kinds of price.
- **2026-09-08** — **Stopped at the declared cut line.** The migration, the API and the facility UI — price list and apólice section — are built and tested. The student-facing half is next: a seguro fee line giving one student a coverage period, pro-rata for a mid-season joiner, the renovação price chosen by default for a returning student, and the no-cover badge on the student page and in the register.

## Round 9 — two fixtures that lied, and a date that named the wrong day

- **2026-09-08** — **The three date-dependent `fees.integration` tests are pinned rather than left as a known failure.** They set `payment_due_day = 8` on a line starting on the 5th and asserted `isOverdue` — which is `fee_due_on(current period start, due day) < current_date` — so the current occurrence was due on the 8th of *this* month and the assertion was simply false from the 1st to the 8th. They failed for a week a month and passed for three, which is a fixture that cannot tell a regression from a Tuesday. Fixed by starting the line on the **28th**: a monthly occurrence beginning on the 28th started this month if today is the 28th or later and last month otherwise, and the 8th of that month is behind us either way. One constant, `OVERDUE_SINCE`, so the next test asserting lateness does not invent a third date that happens to work in September.
- **2026-09-08** — Not fixed by injecting a clock. Nothing else in this codebase does, the derivation is in SQL against the database's own `current_date`, and a fixture that reads the calendar is the second implementation of the rule the test exists to check.
- **2026-09-08** — **A lesson plan is dated by the day the class is taught and filed by the day the pattern implied.** Those were the same value until a week could be moved; a plan is keyed on `occurs_on` so it survives a regeneration, and a one-week move deliberately leaves `occurs_on` alone — so a Wednesday class moved to Saturday kept a Wednesday key and the sheet said "quarta-feira" over a lesson taught on the Saturday. The key stays where it is and is now internal; what comes out of the API is where the class is. The previous lesson's label is resolved the same way, falling back to the key when its session no longer exists, because a plan whose week was removed is still the last thing somebody wrote.
- **2026-09-08** — Not fixed by keying the plan on the session id or on the real date. The first vanishes on the next regeneration, which is the failure the date key exists to prevent; the second collides the moment two lessons of one turma are moved onto the same day. Both halves are asserted in one test — the screen must say Saturday *and* the row must still be filed under Wednesday — because fixing one by breaking the other is the obvious wrong repair.

## Round 9 — the student half: what it costs to join, and who is insured

- **2026-09-08** — **`student_fee.fee_period_id` becomes nullable, and null means "charged once".** A mensalidade and a quota are charged every *something* and the period is how the line knows what one occurrence is worth and when the next falls due. An inscrição is paid once for a season; a seguro is bought once for the season it covers. Forcing one to name a period makes the arithmetic *wrong* rather than merely redundant — the total is `amount × months`, so a €12,00 seguro filed against an "Anual" period reads as €144,00. A CHECK confines the shape to the two kinds that mean it, and every row that predates it names a period.
- **2026-09-08** — **The season on a line comes from its plan, never from the caller.** A plan of either season kind names one, and asking a form to restate it is asking two places to agree about something only one of them knows.
- **2026-09-08** — **Pro-rata is a tick, not a rule.** Plenty of clubs charge the whole premium whenever somebody joins, because that is what the insurer charged *them*; which applies is a commercial decision this software has no business making. When it is asked for, the arithmetic runs in SQL against the apólice's own dates and is snapshotted onto the line — the price list is never re-read to rebuild it.
- **2026-09-08** — **A cover line carries its own dates, not the policy's.** Correcting a typo in an apólice must not silently rewrite what a family was told they had, for the same reason editing the price list never rewrites an agreement. The default is the whole of the policy's period, which is what somebody joining in September gets.
- **2026-09-08** — **"Returning" is any fee line in an earlier season, not an earlier inscrição.** A club that only started charging a joining fee this year would otherwise treat every long-standing family as new. Compared by the season's start date rather than its name, which is a label a club may write any way it likes. The renovação price is *pre-selected*, both prices are listed, and an admin may charge either: a default that cannot be overridden is a rule, and this is a judgement.
- **2026-09-08** — **A student with no valid cover is warned about and refused nothing.** The badge appears on the student's page and in the register, and no enrolment, attendance or mark depends on it. An instructor at the poolside cannot fix a seguro, and a register that would not open over a piece of paperwork is a register somebody keeps on paper instead. Three sentences rather than two, because "insurance ended on the 14th" is a renewal and "no insurance recorded" is a conversation with a family.
- **2026-09-08** — The register judges cover against **the class's own day**, not today: a register can be marked late, and "was this child insured when they swam" is the question that matters.
- **2026-09-08** — **A second charge for one season is a 409 naming the plan**, not a 500 quoting an index. Found by the test that asserted the refusal — the constraint fired correctly and the answer reaching the screen was an error page. Matched on the constraint name rather than on the input, because the database is what knows and a check written in TypeScript would still lose the race between two clicks.
- **2026-09-08** — **The register's "pago" tick reaches a one-off line.** `setStudentPaid` inner-joined `fee_period`, so an inscrição would have been silently left outstanding with nothing on screen to say why. Left join and the same one-occurrence CASE the read path uses.
- **2026-09-08** — **`time-slots.sql` test 6 is fixed rather than left as a known flake.** It looked up "the unarchived season" while test 5 above it adds a *draft* for the next year, which is unarchived too — so the assertion measured whichever season the planner reached first. Recorded as intermittent since 2026-09-05 and by now deterministic. The four lookups in that file now name the season they mean. Verified against this session's starting commit: it failed there identically, so it is not a regression from the fee work.

## Round 10 — the two open questions on POOLSE-23, answered before the code

- **2026-09-08** — **Health and mobility notes are readable by the owner, an admin and any instructor, audited — the same rule the medical notes beside them already have.** This reverses the answer given earlier the same evening, which was "the turma's own instructor", and it was reversed as soon as the existing rule surfaced: slice 1.12 had already considered narrowing `student_sensitive` to an instructor's own turmas and rejected it in writing — a child in the water with epilepsy is a safety matter for whoever is standing at the poolside, and the person covering a colleague's class at short notice is exactly who would be locked out. A mobility limitation is the same class of fact, on the same screen, and if anything the argument is stronger for it. Two sensitive fields side by side under two different read rules would have been a difference nobody could defend and somebody would eventually "fix" in the wrong direction. **The audit log is what makes the open read safe, and it stays unconditional.**
- **2026-09-08** — Superseded, kept for the reasoning: **health and mobility notes readable by the owner, an admin, and the instructor responsible for a turma this student is enrolled in.** The ticket left it open between Owner/Admin only and Owner/Admin plus the assigned instructor, and the safety case decides it: somebody running a senior class needs to know about a mobility limitation *before* the person is in the water. Owner/Admin only would not have kept the note private — it would have moved it onto a piece of paper by the poolside, which is worse for the swimmer and worse for the record. Not "any instructor": that turns a sensitive note into a club-wide read for people who never teach this student.
- **2026-09-08** — With the rule matched to the existing one it is a plain `requireRole('owner', 'admin', 'instructor')` on the endpoint, and no row-level resolution is needed. Enforced on the endpoint either way, never by a screen omitting the field — and the read stays the audited event it already is.
- **2026-09-08** — **Adult students get no app login in v1; they are reachable by email.** AC 7 asks only that an adult student can be reached directly, which email satisfies. Student and guardian accounts are phase 3's, and building one here would pull in Clerk invitations, a student role with its own permission surface and a self-service view — most of phase 3, arriving early and needing to be reconciled with it later.

## Round 10 — the adult and senior path

- **2026-09-08** — **POOLSE-23 is built as the record, not the whole ticket.** Landed: the adult-path definition, the consent-form guard, mobility notes and the emergency contact. Left out and why — **AC4** (a fee category on turma or enrolment) is a new concept on a price list reshaped four hours earlier and belongs with the invoicing work; **AC6** (one record, both badges for an adult student who is also an EE) is a students-list change rather than a record change; **AC7** (communications routed to the student) has nothing to route, because `notifications/` sends invitation and vacation email and there is no messaging feature. Writing a routing rule ahead of the feature it routes for would be a rule nobody could test.
- **2026-09-08** — **There is no `is_adult` column and there must never be one.** An adult is a student at or above the club's `age_of_majority` with no live guardian link — the absence of the edge *is* the definition. `student_is_adult_path` says it once, in SQL, STABLE rather than IMMUTABLE because it reads `current_date` and a cached answer would be wrong on somebody's birthday. Every screen reads that one answer: the ticket names "deciding the branch in the client from scattered fields" as the thing most likely to be got wrong.
- **2026-09-08** — **An adult who has a guardian is on the guardian path**, and a student with **no birth date** is too. The first is what the edge-based definition is *for* — a supported adult. The second is a deliberate asymmetry: guessing adult for missing data skips the guardian block for a child nobody has finished registering, which cannot be recovered from, while guessing the other way asks for a guardian an adult is then corrected out of.
- **2026-09-08** — **The consent form is chosen by the server and validated on the way back, with a 422.** Not a 400: the request is well-formed, and what is wrong is the claim it makes about who signed — which the server can check and the client cannot. It runs both ways, because a guardian signing for an adult who has none is the same wrong claim. `signedBy` is optional and absent means "the client did not say", so every caller that predates it goes on working.
- **2026-09-08** — **The emergency contact is a person link or free text, never both, and choosing one clears the other** rather than the CHECK refusing the pair. The two are alternatives and the operator has just chosen one; making them delete the other first is asking them to tidy up after a rule they cannot see. A phone with no name and no person is refused — that is not a contact.
- **2026-09-08** — **The contact picker lists every active membership, not a page.** `People` already carries the argument in its own comment: a picker built from one page offers only the people who landed on it, and the operator concludes the person is not in the system and types the name by hand. Returned only to a caller who may write the contact — an instructor reading a student's notes has no business with the club's staff list.

## Round 10 — the fee category, the ladder and both badges

- **2026-09-08** — **POOLSE-23 is closed except AC7.** AC4, AC5 and AC6 landed after the record did. AC4 was deferred earlier in the evening on the argument that it was "a new concept on a price list that has just been reshaped" — that was wrong on re-reading the ticket: a fee category deliberately touches `fee_plan` not at all. It is a reference the pricing engine will consult, and expressing it costs one table and two nullable columns.
- **2026-09-08** — **`fee_category` is a table, not an enum.** The standing rule decides it: an enum where only a developer changes the set, a table where an operator might, and a club that starts a bombeiros discount in March should not wait for a deploy. Unique per organization on `lower(strip_accents(name))`, because "Senior" and "Sénior" are one word to a club.
- **2026-09-08** — **A category is a reference, never a percentage.** What one is *worth* belongs to the pricing engine, which is not built. A number typed into a form now would be a discount nobody can report on and nobody can change in one place — and it would sit beside `fee_plan` pretending to be a price. The consequence worth having: renaming a category reaches every turma and enrolment at once, because they hold its id.
- **2026-09-08** — **The enrolment beats the turma, and `enrolment_fee_category` is that sentence in SQL.** One definition, for the reason `fee_total_cents` is one. Clearing a person's own category returns them to their turma's rather than to none — which is why that clear is a DELETE rather than a PATCH carrying a null, since "whatever the turma says" is the state worth being able to return to.
- **2026-09-08** — **`fee_category` sits after `class_group` in `TENANT_TABLES`.** Both a turma and an enrolment point at one and neither reference is cascaded, so deleting the categories first fails on the foreign key. Found by the first test run, which is what that list is for.
- **2026-09-08** — **Reading the category list is open to instructors; writing is not.** It is a label printed beside a turma's name and says nothing about money — unlike `fee_plan`, which is owner and admin even to read.
- **2026-09-08** — **A dual-role person is one record with two badges, and "guardian" is an *outbound* edge.** The avó who swims on Tuesdays and brings her granddaughter on Thursdays appears once, because Alunos is a list over `student`. The trap the ticket names is the query: asking "does this person have any guardian edges at all" finds the ones she holds *over* her granddaughter, takes her off the adult path and addresses her own consent form to a parent she does not have. Asserted from both ends in one test.
- **2026-09-08** — **AC5 needed no code**, only a test. Senior levels already sit in the one ladder with the same `sort_order` and the same age bounds; the claim was simply unasserted, and POOLSE-19's "next level" logic depends on it staying true.

## Round 10 — the categories screen, and a field that was never being sent

- **2026-09-08** — **The fee categories get a screen at Alunos → Categorias**, beside Níveis. Both are lists a club sets up once and rarely touches; putting either on a daily screen would give the daily job a settings panel it does not need. Organization-scoped, because a "Sénior" that meant one thing at one pool and another at the next is a category nobody could report on.
- **2026-09-08** — **The turma's category moves into the turma's own form, and `GroupCategoryController` is deleted.** It was written a few hours earlier as a dedicated endpoint, which was wrong once the field had a home: a turma's category is a fact about it like its level and its pool, and a second write path for one field is how two screens end up disagreeing about what was saved. The *enrolment* endpoint stays — an enrolment has no form of its own, and "this person, not their turma" is a decision taken from the student's record.
- **2026-09-08** — **`groupBody` in `classes.actions.ts` was never sending `colour`, so a turma's colour has been silently dropped on every save since round 6.** The swatches rendered, an operator picked one, the value never left the browser, the API defaulted it to null and the calendar went on using the level's tint. Nothing errored because nothing was wrong — the field simply was not sent. Found while adding the category to the same hand-written list of FormData keys, which nothing typechecks against the form.
- **2026-09-08** — Guarded by an integration test asserting that a turma keeps **both** its colour and its category across an update, and by a comment on `groupBody` saying that a control added to the form must have its name added there in the same commit. A type cannot catch this — the list is strings — so the guard is a test plus a sentence in the one place somebody will be standing when they add the next field.

## Round 10 — the enrolment override gets its control, and the dropped-field class is swept

- **2026-09-08** — **The enrolment's own category is set from the turma's roster**, beside the person it belongs to. It is an exception made for one member of a group, so it is set where that person is listed rather than on a screen of their own. Saves on change: one field with three or four values, where a Save beside it would be a second click for a decision already made — the same reasoning as the calendar's teacher picker.
- **2026-09-08** — **The picker's empty option reads "Da turma", not "Sem categoria"**, and names the turma's category where there is one. Clearing an override returns somebody to their turma's, which is a different state from being on no category at all — and the option that says which is which is the one an operator can act on. It is a DELETE rather than a PATCH with a null, so the two states stay distinct on the wire as well as on the screen.
- **2026-09-08** — **The effective category is resolved in SQL and shipped**, so the roster prints the answer that applies without knowing the precedence. The same rule as every other derived answer here: one definition, and the client renders it.
- **2026-09-08** — **The `colour` class of bug was swept across the app and nothing else was found.** A form control posts under a name; an action builds its body from a hand-written list of keys that nothing typechecks against the form; a missing name is a value dropped on every save. Compared every `name="…"` on a form against the strings in the action files beside it: three hits, all explained — `costDisplay` is a display-only input feeding a hidden field by design, and the two on the ownership transfer are read by an action in the parent folder. Not kept as a repo check: at its narrowest setting it was three for three false positives, and a check that only cries wolf gets ignored. The guard stays the test plus the comment on `groupBody`.


## Round 11 — the four questions invoicing could not be started without

- **2026-09-08** — **These are internal records, shaped so certification is additive.** Asked before any code, because it decides everything under it. Decreto-Lei 28/2019 wants certified software, a validation code obtained from the AT per series, an ATCUD and a QR on every document and a SAF-T (PT) export — a programme with an external dependency, not an evening. So Poolse produces a numbered, immutable record a club hands to whatever issues its faturas, and the screens say so in both languages rather than leaving a club to hear it from its accountant. Two columns are reserved and null — `invoice_series.at_validation_code`, `invoice.atcud` — and they are the only speculative thing in the module. What is *not* speculative is the shape, because the shape is what cannot be retrofitted onto documents a family has already been sent.
- **2026-09-08** — **A numbering series belongs to a facility.** A club running two pools numbers each site's documents in its own book, and the answer is not recoverable later without renumbering. The licence already bounds how many sites a subscription has, so for almost every tenant this is one book hanging off the one facility — and the club that grows into two does not have to renumber. The *prefix*, though, is unique per **organization**: the club is one legal entity and two sites both numbering `FT A/1` would issue two different documents under one number, so a second site takes the first free suffix and can be renamed while its book is empty.
- **2026-09-08** — **A monthly run and a per-student action, and they are one call.** The second is the first with `studentIds` set. Two entry points would be two queries and eventually two answers about what a month owes; a club with 200 students will not click 200 times, and a joiner on the 12th cannot wait for next month.
- **2026-09-08** — **One document per payer, so siblings land together.** The payer is the primary guardian, or the student themselves where there is no guardian — the adult path, and also a student nobody has finished registering. A family paying one amount by débito direto in 2.3 needs one document to collect against, and the payer relationship is the expensive half to retrofit onto numbers already issued. Lines keep their `student_id`, so a per-student view is a filter rather than a different document.

## Round 11 — the numbering, and the four ways it is kept honest

- **2026-09-08** — **`next_number` is a column, not a Postgres sequence, and the difference is the whole module.** A sequence is not transactional: a rolled-back insert consumes its number and leaves a gap, which is the one thing a numbering series may not do. The `UPDATE … RETURNING` that reads and bumps the column rolls back with its transaction. `invoices.sql` test 3 asserts exactly that, and it is the test to keep if the file is ever cut down — a well-meaning refactor to a sequence passes every other assertion in it.
- **2026-09-08** — **The allocation lives in a BEFORE INSERT trigger on `invoice`, and an INSERT that supplies a number is refused.** The prompt asked for numbering that is structurally safe rather than carefully written, and this is what that means: the allocation and the row it numbers are one statement, so application code cannot hold a number it did not use, cannot retry and skip one, and cannot be written tired in a way that does either.
- **2026-09-08** — **The row lock that allocation takes is load-bearing beyond numbering.** It serialises every insert into one series, which is what makes the double-billing constraint trigger sound under two people pressing the button at once. Written down because it looks like a side effect and is in fact the reason a constraint trigger was allowed at all.
- **2026-09-08** — **A document is written once, said with a privilege rather than a trigger.** `poolse_app` holds SELECT and INSERT on `invoice` and `invoice_line` and nothing else — a revoke, since `core-tenancy` grants all four by default. A trigger would do the same job and could be dropped by a later migration that meant something else; a missing privilege cannot be forgotten by application code at all. Same family as `audit_log` and `consent`, which the harness already cleans up over the owner's connection for the same reason. There is therefore no `updated_at` and no `archived_at` on either table: a column nothing can ever change is a lie. Settlement, in 2.3, arrives as a child table exactly as `student_fee_payment` did — not as a column on a document.

## Round 11 — one charge per occurrence, and the half everybody forgets

- **2026-09-08** — **"Billed twice" is refused by a constraint trigger, not a partial unique index.** The index is the obvious instrument and it is wrong: after a credit note the occurrence is chargeable again, and that is the ordinary way a club fixes a document it got wrong. "Live" means "not since credited", which needs a join a partial index cannot do. The test that matters is the pair — the second charge refused, and the same charge allowed once the credit note exists.
- **2026-09-08** — **`invoice_charged_on` is one definition with two readers**: the trigger that refuses a second charge and the monthly run that must not offer one. Written twice they agree until somebody fixes a bug in one of them, and a preview offering what the commit then refuses is a failure the operator cannot see the cause of. It takes an `except_line_id` so the AFTER-INSERT trigger can ask about a row already inserted; excluding by line rather than by document also catches one occurrence written twice onto one document.
- **2026-09-08** — **A credit note is whole-document, not per line.** Partial credits are expressible later without breaking anything — each credit line already names the invoice line it reverses — and whole-document is what a small club actually does. One credit note per document, because a document credited twice makes "has this been corrected" have two answers and doubles anything summed from the pair.
- **2026-09-08** — **A credit note is numbered in its own book.** Sharing one series between faturas and credit notes would make each type's sequence gappy, which is the thing the series exists to prevent. The kind travels into the composite key `(organization_id, series_id, kind)`, so a fatura cannot be numbered in the credit note book — a key rather than a trigger, because a key cannot be raced.

## Round 11 — what a line may say, and what it may not

- **2026-09-08** — **The line stores the club's own words and never the translated kind.** Discovered by the tests rather than by reading: `fee_plan` has no `name` column at all — a plan's label is its kind, its level and its frequency, composed on the client. So `description` holds the level's or the season's name, `lessons_per_week` holds the frequency, and `kind` stays an enum whose Portuguese is an i18n key. A document storing "Mensalidade" would read half in Portuguese for a club working in English, and would be frozen at the language of whoever pressed the button. `description` is nullable because a plain quota has no such word, and a NOT NULL would have forced one to be invented.
- **2026-09-08** — **`invoice_line.student_tax_number` is not a duplicate of the payer's.** `student.tax_number` has existed since round 8 precisely because a parent deducting lessons on their IRS does it against the *child's* number — so a document addressed to a guardian routinely carries a different number per line, which one column on the document could not express for a family with two children. (A question was asked about where an adult's own NIF lives; the column already existed, with a better argument than the one proposed. No gap.)
- **2026-09-08** — **Totals are summed in SQL and not stored.** The lines cannot change, so a stored total could not drift either — but this way there is one definition rather than two, and `invoice_vat_cents` is the same function the preview calls, so a run and the document it became show the same tax.
- **2026-09-08** — **A run's occurrence is derived from the line's own start walked forward by its periodicity**, not from `current_period_start`. That function answers "what is this line being asked for *today*", and a club invoicing March in April needs March. A line charged once has exactly one occurrence, on its `starts_on`, and every join to `fee_period` is a LEFT JOIN — an inner one drops every inscrição and every seguro from the run, which is the failure the register's "pago" tick already found once.
- **2026-09-08** — **The count of occurrences left out is reported, not dropped.** "There is nothing to bill" and "everything here is already billed" look identical on an empty table and are different things for an operator to do. Silence is what makes a run read as a feature that did nothing — the same lesson as round 7's `weeksKept`.

## Round 12 — phase 3 moves to last, and what that costs

- **2026-09-08** — **The student app and notifications are built after phases 4, 5 and 6.** Rui's call: the backoffice is what a club pays for, phase 1 was designed to be independently valuable and is finished, and a second app is the largest single piece of work left serving people who are not the customer. Taken as a decision rather than met as drift, so the consequences are written down once instead of one at a time.
- **2026-09-08** — **4.2 is the only real dependency, and it is not a blocker.** "An out-of-range reading reaches someone" needs delivery, and `sendEmail` already carries invitation and vacation email — so an alert goes out by transactional email and the reading is flagged on the pool's own page besides. What 3.0 adds later is per-person preferences and push, which is an upgrade rather than a prerequisite. POOLSE-26 is the same shape and takes the same answer.
- **2026-09-08** — **Collections move with 3.5, and POOLSE-24 and POOLSE-25 with them.** A SEPA mandate and an MB WAY authorisation both need the payer to act, and the payer has no account until phase 3. Chasing therefore stays manual for longer — which is exactly what 2.3 built, so nothing is wasted. 2.4 is unaffected: the club paying Poolse involves somebody who already has a login.
- **2026-09-08** — **3.6 stops being a problem.** The roadmap had flagged "water temperature in the app depends on phase 4" as a known reorder risk. With phase 3 last, the conflict is simply gone — the one place where this reordering makes the plan simpler rather than more complicated.

## Round 12 — settlement, and the state nobody stores

- **2026-09-08** — **A payment is a child row, never a column on the document**, as 2.2 promised. `invoice` holds no UPDATE grant for the app role, so a `settled_on` column could not have been written even by somebody who wanted one — but the honest reason is that a family paying half in October and half in November is two facts, and a single date holds neither. `payment_source` is reused from the fee register rather than reinvented: a second enum meaning the same thing in different words is how two screens end up disagreeing about what `sepa` is.
- **2026-09-08** — **There is no status column, and `invoice-settlement.sql` test 2 asserts the schema has not grown one.** Every input is already stored — `total − paid`, `due_on` against today, whether a credit note exists — and a fifth fact restating the other four would need a worker to keep it true, which is a per-tenant cost. `invoice_status` is that derivation, in SQL, STABLE because it reads `current_date`. The same argument as the overdue-cleaning rule in `spaces.repository.ts`.
- **2026-09-08** — **The precedence is credited, then paid, then overdue.** A partly paid document past its due date is still overdue — half of nothing arriving on time is still late — and a credited document is owed by nobody even where money had already arrived: the payment stays on the record, the debt does not. Both are the kind of rule somebody "simplifies" by reading the CASE from the bottom, so both are asserted.
- **2026-09-08** — **`invoice_dates_ordered` is dropped, and nothing replaces it.** 2.2 wrote `CHECK (due_on >= issued_on)` on the assumption that a document is issued before it falls due. That is wrong for the ordinary case 2.3 exists to serve: bill October on the 2nd of December and the run dates the document today with a due date six weeks behind, and the constraint refused it as a bare `23514` naming no field. There is no ordering rule between those two dates that is true of every real document, and a constraint that is right most of the time fails on the case somebody is actually in.
- **2026-09-08** — Found by the first integration test that tried to produce an overdue document, which is the only way to produce one. Worth recording because it is the second time this module's tests have caught something `tsc` could not see — the first was `fee_plan` having no `name` column at all.
- **2026-09-08** — **An overpayment settles rather than owing the family money.** `outstandingCents` floors at zero: a negative figure on a chase list is a number somebody would try to collect. A rounding cent over is not a case worth modelling as a refund.
- **2026-09-08** — **A payment is soft-deleted.** An entry against the wrong document has to be removable, and a hard DELETE would take the record of the mistake with it. Every sum filters `archived_at`, and the test asserts the archived row is still there.
- **2026-09-08** — **A credit note is never paid, refused by a trigger rather than by a screen.** There is a second way in as soon as anything reconciles a bank feed, and a rule that lives in one form is a rule the other path does not have.

## Round 12 — chasing, without pretending to send anything

- **2026-09-08** — **A chase is a record of what a person did, not a message Poolse sent.** With phase 3 last there was no honest alternative, and the fallback turns out to be the better shape anyway: what an operator needs before picking up the telephone is whether this family has been asked once or three times and what they said last time. The screens say plainly that nothing is sent — a club that believed this emailed the family would stop telephoning them, which is the worst outcome of a hopeful label.
- **2026-09-08** — **The channel is an enum on the row, so 3.0 writes into the same history later** rather than starting a second one. That is the whole reason it is `invoice_chase` with a channel rather than `invoice_manual_reminder`.
- **2026-09-08** — **The chase list is what is *outstanding*, not what is overdue.** A club working through its debtors on a Wednesday wants Friday's document in front of it too; a list that appeared only once the date had passed is a list nobody can get ahead of. Sorted oldest debt first, because it is a job to work through rather than a record to look something up in — the opposite of the month view beside it, which is newest first.
- **2026-09-08** — **The chase list shows what is *outstanding*, never the total.** A family that has paid half owes half, and the total on its own would send somebody to ask for the whole amount. `lastChasedOn` is beside it for the same reason: telephoning somebody who was telephoned yesterday is the failure the column exists to prevent.

## Round 13 — the enrolment QA batch (2026-09-08)

- **2026-09-08** — **Unticking sócio removes the quota line, but only while it is unpaid and un-invoiced** — F-01. The add was automatic, so the removal has to be; a toggle that attaches a charge and cannot detach it left a family paying 4,00 € a month for a membership they had given up, and the line survived a reload, which reads on screen as a save that did not happen. Two things stop it and both are history: a line marked paid, and a line on an issued document whose invoice has not since been credited. Archiving either would take money out of a total already reconciled or already sent to a family, so those keep the line and the screen says why — arriving at the "quota dispensada" case from the other direction.
- **2026-09-08** — **"The live quota line" is the set on both sides, and no provenance column was added.** The add already refuses to create a second line when any live quota exists, so removal over the same set is what makes tick → untick → tick land back where it started. A column recording "this line came from the toggle" would be a schema change for a distinction the paid and invoiced guards already cover; flagged rather than taken.
- **2026-09-08** — **Switching the membership off never depends on the fee line, and removing the fee line never touches the membership.** They are two facts, and an honorary member — sócio with the quota waived — is the case that proves it. Asserted from both ends.
- **2026-09-08** — **A NIF is checksum-validated app-wide, reversing the written rule that said it never would be** — F-02. The old comment on `students.controller.ts` and `membership.tax_number` argued that an operator copying a number off a form should not be stopped by a checksum, and that a wrong-but-plausible number is a correction rather than a crash. QA showed the cost: `134167211` went in on a student *and* on their guardian in one submit, and the duplicate-person guard is *keyed* on the NIF — so a number that cannot exist silently defeats the thing meant to stop one club holding two records for one person. `isValidNif` lives in `@poolse/rules`, with the conflict rules, because a form that accepts what the API refuses is the failure that package exists to prevent.
- **2026-09-08** — **Three form paths validate it, and the importer deliberately does not.** The student's NIF, an inline guardian's, and a partner's — the partner's previously checked nine digits and no more. The student importer accepts a NIF too and was left alone: it has its own preview-and-confirm where a human sees warnings, and turning a legacy spreadsheet's bad NIF into a hard row error would block an import a club has no other way to do. Adding it as a *warning* there is proposed, not taken.
- **2026-09-08** — **One NIF is one person, checked in the controller.** `student.tax_number` and `membership.tax_number` carry a unique index each, per table, so a child and their mother sharing a number violated neither and nothing between them was looking. The controller is the only layer where a whole submit — one student, several guardians — is visible at once, so that is where the comparison lives; the error names the *guardian's* field, because the student's is the number the operator meant.
- **2026-09-08** — **Not extended to "this NIF already belongs to somebody in the club".** The brief asked for it, and it would break the case the dedupe exists for: an inline guardian whose NIF matches an existing person is *attached* to them, which is what stops the second sibling producing a second mother, and POOLSE-23's dual-role person would fail it too. Flagged rather than taken.
- **2026-09-08** — Four demo/fixture NIFs were impossible numbers and are corrected (`501234567` → `501234560` and three in `seed-reference.ts`). They bypass the API, so nothing was failing — but a club editing that partner through the form would now be refused by data Poolse itself wrote.
